name: Build and Push Images

on:
  push:
    branches:
      - prod

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create application-secret.yml
        run: |
          mkdir -p backend/src/main/resources
          echo "${{ secrets.BE_APPLICATION_SECRET }}" > backend/src/main/resources/application-secret.yml
        shell: bash

      - name: Create .env
        run: |
          echo "${{ secrets.FE_APPLICATION_SECRET }}" > frontend/.env
        shell: bash

      - name: Verify secret file exists
        run: |
          test -f backend/src/main/resources/application-secret.yml|
          test -f frontend/.env
        shell: bash

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      # =====================
      # Backend Secret Inject
      # =====================
      - name: Create application-secret.yml
        run: |
          mkdir -p backend/src/main/resources
          echo "${{ secrets.BE_APPLICATION_SECRET }}" > backend/src/main/resources/application-secret.yml

      # =====================
      # Backend Image
      # =====================
      - name: Build & Push Backend Image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/goorm-comma-be:latest

      # =====================
      # Frontend Image
      # =====================
      - name: Build & Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/goorm-comma-fe:latest
