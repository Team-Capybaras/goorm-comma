spring:
  datasource:
    url:
    username:
    password:
    driver-class-name:

  jpa:
    hibernate:
      ddl-auto: create-drop # 개발용, 실행 시 테이블 초기화
    show-sql: true # 개발용, 런타임에서 로그 보기 어려우면 false로 비활성화

  flyway:
    enabled: true
    locations: classpath:db/migration